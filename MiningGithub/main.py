from experiment_1 import new_experiment_1

ACCESS_TOKEN = ''
USER = 'PyGithub'
REPO = 'PyGithub'

MAX_COMMITS = 10

new_experiment_1(token=ACCESS_TOKEN,
                 user=USER,
                 repo=REPO,
                 max_commit=MAX_COMMITS)
